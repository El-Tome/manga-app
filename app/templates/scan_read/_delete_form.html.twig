<form method="post" action="{{ path('app_scan_read_delete', {'id': scan_read.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scan_read.id) }}">
    <button class="btn">Delete</button>
</form>
