{% extends 'base.html.twig' %}

{% block title %}Scan{% endblock %}

{% block body %}
    <h1>Scan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scan.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ scan.name }}</td>
            </tr>
            <tr>
                <th>Chapter</th>
                <td>{{ scan.chapter }}</td>
            </tr>
            <tr>
                <th>Language</th>
                <td>{{ scan.language }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_scan_index') }}">back to list</a>

    <a href="{{ path('app_scan_edit', {'id': scan.id}) }}">edit</a>

    {{ include('scan/_delete_form.html.twig') }}
{% endblock %}
